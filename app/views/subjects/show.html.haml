%p#notice= notice

%p
  %b Name:
  = subject.name
%p
  %b Short name:
  = subject.short_name
%p
  %b Description:
  = subject.description

= button_to 'Edit', edit_subject_path(subject)
= button_to 'Back', subjects_path
%br
=button_to 'Show all items', subject_subject_items_path(subject.id)

=button_to "Add new subject item", new_subject_subject_item_path(subject.id)
	
