%p#notice= notice

%p
  %b Name:
  = subject.name
%p
  %b Short name:
  = subject.short_name
%p
  %b Description:
  = subject.description

%br
.pagination
  = link_to 'Edit', edit_subject_path(subject)
  = link_to 'Back', subjects_path
  = link_to 'Show all items', subject_subject_items_path(subject.id)
  = link_to "Add new subject item", new_subject_subject_item_path(subject.id)
	
